{
  "name": "wdb-discord-bot",
  "version": "1.0.0",
  "description": "A custom Discord bot for the WDB server",
  "main": "src/server.ts",
  "repository": "https://github.com/wdbears/wdb-discord-bot.git",
  "author": "Ahmad Raza <razatastic@gmail.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "start": "nodemon src/server.ts",
    "start-deploy": "yarn deploy && nodemon src/server.ts",
    "build": "tsc",
    "serve": "node dist/server.js",
    "deploy": "yarn build && node dist/deploy.js",
    "start-live-prod": "export NODE_ENV=production && nodemon src/server.ts",
    "start-prod": "export NODE_ENV=production && yarn prisma generate && yarn deploy && yarn serve",
    "deploy-prod": "export NODE_ENV=production && yarn deploy",
    "clean": "rm -rf node_modules dist",
    "clean-install": "yarn clean && yarn install && yarn prisma generate",
    "lint": "eslint src/**/* && yarn prettier --write ."
  },
  "dependencies": {
    "@discordjs/builders": "^0.11.0",
    "@discordjs/rest": "^0.2.0-canary.0",
    "@prisma/client": "^3.8.1",
    "cheerio": "^1.0.0-rc.10",
    "discord-api-types": "^0.26.1",
    "discord.js": "^13.6.0",
    "dotenv": "^14.2.0",
    "express": "^4.17.2",
    "node-fetch": "3.1.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.9",
    "@typescript-eslint/eslint-plugin": "^5.10.0",
    "@typescript-eslint/parser": "^5.10.0",
    "eslint": "^8.7.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-import": "^2.25.4",
    "eslint-plugin-prettier": "^4.0.0",
    "husky": "^7.0.4",
    "lerna": "^4.0.0",
    "lint-staged": "^12.1.7",
    "nodemon": "^2.0.15",
    "prettier": "^2.5.1",
    "prisma": "^3.8.1",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{ts,css,md}": "prettier --write"
  }
}